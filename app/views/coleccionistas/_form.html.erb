<%= form_for(@coleccionista) do |f| %>
  <% if @coleccionista.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coleccionista.errors.count, "error") %> prohibited this coleccionista from being saved:</h2>

      <ul>
      <% @coleccionista.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :nombre, t(:name)  %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
